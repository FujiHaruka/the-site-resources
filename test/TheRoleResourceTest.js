/**
 * Test for TheRoleResource.
 * Runs with mocha.
 */
'use strict'

const TheRoleResource = require('../lib/TheRoleResource')
const { ok, equal } = require('assert')

describe('the-role-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
