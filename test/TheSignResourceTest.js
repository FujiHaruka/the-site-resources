/**
 * Test for TheSignResource.
 * Runs with mocha.
 */
'use strict'

const TheSignResource = require('../lib/TheSignResource')
const { ok, equal } = require('assert')

describe('the-sign-resource', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
